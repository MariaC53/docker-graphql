name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        
    - name: Git Semantic Version
      uses: PaulHatch/semantic-version@v4.0.3
      with: 
        major_pattern: "major:"
        minor_pattern: "feat:"
        format: "${major}.${minor}.${patch}-prerelease${increment}"
      id: version
      
    - name: Docker login
      env:
        DOCKER_USER: ${{ secrets.DOCKER_USER }}
        DOCKER_PSWD: ${{ secrets.DOCKER_PSWD }}
        NEW_VERSION: ${{ steps.version.outputs.version }}
      run: |
        docker login -u $DOCKER_USER -p $DOCKER_PSWD
        echo "New version: $NEW_VERSION !!!!!!!!!!!!!"

    
    
    # - name: Build the Docker image
    #   run: |
    #     docker build --tag msalas53/docker-graphql:0.0.2 .
    #     docker build --tag msalas53/docker-graphql:latest .

    # - name: Push Docker image
    #   run: |
    #     docker push msalas53/docker-graphql:0.0.2
    #     docker push msalas53/docker-graphql:latest
